{
  "name": "hountybunter-monorepo",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "workspaces": ["core", "adapters/*", "surfaces/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "hb": "tsx surfaces/cli/src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
